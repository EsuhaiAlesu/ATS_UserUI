# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ONLINE lane saved transcripts (server writes here)
translated_history/
online-keys.json
# ONLINE lane store, when DATA_DIR is not set (dev clone). Holds the operator's own glossary and the
# per-meeting boxes — a `git add -A` on a machine that ran the server without a disk attached would
# otherwise put that in the history.
online-data/
